import { P, SettingsDocScreen } from "../../src/components/settings/SettingsDocScreen";

export default function SettingsFoldersPlaceholderScreen() {
  return (
    <SettingsDocScreen title="Folders">
      <P>
        Organising reflections into folders is on the roadmap. For now, your library stays sorted by recency on
        the Reflect tab and aligned with the web app when you sign in.
      </P>
    </SettingsDocScreen>
  );
}
