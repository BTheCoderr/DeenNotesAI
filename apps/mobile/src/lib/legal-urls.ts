/**
 * Canonical production legal & support URLs (App Store submission).
 * Do not use alternate domains in app UI or paywall fallbacks — ASC metadata should match these.
 */
export const CANONICAL_SITE_ORIGIN = "https://deennotesai.netlify.app";

export const LEGAL_PRIVACY_URL = `${CANONICAL_SITE_ORIGIN}/privacy`;
export const LEGAL_TERMS_URL = `${CANONICAL_SITE_ORIGIN}/terms`;
export const LEGAL_CONTACT_URL = `${CANONICAL_SITE_ORIGIN}/contact`;
